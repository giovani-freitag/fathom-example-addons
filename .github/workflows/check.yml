name: check

on:
  push:
    branches: [main]
  pull_request:

# A reading that does not compile against Fathom's real surface is a reading
# nobody can open, so this runs on everything.
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      # `npm run check` starts by confirming the vendored surface is the one
      # Fathom offers, so a reading cannot pass against a copy that has moved on.
      - run: npm run check
      - run: npm run build
      - uses: actions/upload-artifact@v4
        with:
          name: bundles
          path: dist/bundles
